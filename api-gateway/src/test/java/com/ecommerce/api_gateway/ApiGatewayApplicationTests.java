package com.ecommerce.api_gateway;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * This class contains the basic context loading test for the API Gateway application.
 * It ensures that the Spring application context is loaded correctly during test execution.
 * This is a default sanity check generated when creating a Spring Boot project.
 */
@SpringBootTest
class ApiGatewayApplicationTests {

	/**
	 * This test verifies that the Spring application context loads successfully.
	 * If the application fails to start due to configuration or bean wiring issues,
	 * this test will fail.
	 */
	@Test
	void contextLoads() {
		// No assertions needed; failure to load context will cause this test to fail.
	}

}
